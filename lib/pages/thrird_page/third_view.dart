import 'package:flutter/material.dart';

class ThirdPage extends StatelessWidget {
  const ThirdPage({super.key});
  static String route = "/third";
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
